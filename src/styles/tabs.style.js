import { StyleSheet } from "react-native";

export const tabBarInactiveTintColor = '#FFF'
export  const tabBarActiveTintColor = '#FF0000'
export const styles = StyleSheet.create({
        tabBarStyle: {
                position: 'absolute',
                backgroundColor: '#00A36E',
                bottom: 15,
                marginRight: 15,
                marginLeft: 15,
                borderRadius: 30,
                zIndex: 10,
        },
});